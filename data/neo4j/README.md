# Neo4j Data Directory

Local data storage for Neo4j database.

This directory is mounted as the Neo4j data volume for local development.

## Files

- `databases/` - Neo4j database files
- `transactions/` - Transaction logs

## Note

This directory should be added to `.gitignore` in production.
